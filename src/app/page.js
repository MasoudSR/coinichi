import MainPage from "@/components/MainPage";

export default function Home() {
	return (
		<main className="min-h-screen flex justify-center flex-col bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-red-300 to-indigo-500">
			<MainPage />
		</main>
	);
}
